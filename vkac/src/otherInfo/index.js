import React from "react";

const GPACalculator = () =>{
 const percentage = 68;
 const gpa = (percentage / 25); 
 return (<>
    <div>In the United States, GPA (Grade Point Average) is typically calculated on a scale of 4.0.</div>
 </>);
};

export default GPACalculator;